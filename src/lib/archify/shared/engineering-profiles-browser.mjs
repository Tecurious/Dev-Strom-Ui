/** Browser-vendor stub for tt-a1i/archify engineering-profiles.mjs (MIT).
 * The deployment-ownership profile is opt-in and chartographer never sets it. */
export function validateEngineeringProfile(_diagram) {
  return [];
}
