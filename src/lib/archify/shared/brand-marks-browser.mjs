/**
 * Browser-vendor stub for tt-a1i/archify brand-marks.mjs (MIT).
 * Brand identity is optional; chartographer specs never set `brand`, so the
 * node-only implementation (crypto/dns pinning) is stubbed here.
 */
export function brandLabelFitWidth(_c, width) {
  return width;
}
export function brandMetadataFor(_c) {
  return {};
}
export function brandTopRailProblem(_c, _width, _pad, _context) {
  return null;
}
export function renderBrandMark(_c, _pos) {
  return '';
}
export async function prepareDiagramBrandMarks(_diagram) {
  return { marks: new Map() };
}
